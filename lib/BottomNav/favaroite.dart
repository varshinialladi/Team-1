import 'package:flutter/material.dart';

class FavaroitePage extends StatelessWidget {
  const FavaroitePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: Center(child: Text("favarotie Page")));
  }
}
